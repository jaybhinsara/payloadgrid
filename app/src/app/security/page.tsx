import type { Metadata } from "next";
import { Fingerprint, KeyRound, LockKeyhole, Network, ShieldCheck, Users } from "lucide-react";
import { PublicPage } from "@/components/marketing/public-page";
export const metadata: Metadata = { title: "Security and Data Handling", description: "PayloadGrid security controls, data retention, provider verification, and current beta boundaries." };
const controls = [{ icon: Fingerprint, title: "Signed deliveries", copy: "Endpoint-specific HMAC-SHA256 signatures include a delivery ID and timestamp." }, { icon: ShieldCheck, title: "Provider verification", copy: "Raw-body validation for Razorpay, Cashfree, Stripe, and Shopify callbacks." }, { icon: KeyRound, title: "Hashed API keys", copy: "API key secrets are shown once. Only SHA-256 hashes are retained for authentication." }, { icon: LockKeyhole, title: "Encrypted provider secrets", copy: "Provider verification secrets use AES-256-GCM application encryption before database storage." }, { icon: Users, title: "Tenant boundaries", copy: "Organizations own projects; every application, endpoint, key, message, and delivery query is project-scoped." }, { icon: Network, title: "Destination controls", copy: "Production destinations require HTTPS. Private, loopback, local, and metadata addresses are rejected." }];
export default function SecurityPage() { return <PublicPage eyebrow="Security model" title="Specific controls, clearly stated." intro="Infrastructure trust should be based on implemented controls and visible boundaries, not generic badges."><section className="security-control-grid">{controls.map(({ icon: Icon, title, copy }) => <article key={title}><Icon size={21} /><h2>{title}</h2><p>{copy}</p></article>)}</section><section className="public-section"><span className="section-label">Data handling</span><h2>Short retention by default</h2><p>Webhook payloads, diagnostic request headers, and captured response bodies are retained for three days during public beta and then redacted by scheduled maintenance. Delivery metadata such as event type, timestamps, status, and latency remains available for operational history.</p></section><section className="public-section"><span className="section-label">Current hosting</span><h2>Vercel, Neon, and optional Upstash processing</h2><p>The web application and API run on Vercel, PostgreSQL data is stored in Neon, and durable delivery jobs use Upstash QStash when configured. Deployment region and data location follow the regions selected in those services. PayloadGrid does not currently claim a fixed India-only region.</p></section><section className="boundary-table"><div><strong>Implemented</strong><p>HTTPS destinations, SSRF checks, provider signatures, signed outbound delivery, key hashing, encrypted provider secrets, role checks, audit logs, duplicate protection, rate limits, and payload redaction.</p></div><div><strong>Not claimed during beta</strong><p>SOC 2, PCI DSS certification, penetration-test attestation, static source IPs, regional data-residency guarantees, or a contractual uptime SLA.</p></div></section><section className="public-section"><span className="section-label">Reporting</span><h2>Found a security issue?</h2><p>Do not include live secrets or customer payloads in the first message. Use the contact page with the subject “Security report” and we will coordinate a private channel.</p></section></PublicPage>; }