import { PublicPage } from "@/components/marketing/public-page";
import { WebhookPlayground } from "@/components/marketing/webhook-playground";
import { publicMetadata } from "@/lib/seo";
export const metadata = publicMetadata({ title: "Live Webhook Tester and Request Inspector", description: "Generate a temporary webhook URL, send real HTTP POST requests, and inspect captured webhook headers and bodies without creating an account.", path: "/playground" });
export default function PlaygroundPage() { return <PublicPage eyebrow="Live webhook playground" title="Send a real webhook. Inspect what arrived." intro="Create a temporary public endpoint, POST from cURL or the request builder, and inspect the request captured by PayloadGrid. No account required."><WebhookPlayground /><section className="public-section"><span className="section-label">Real request path</span><h2>A real HTTP request, not a scripted animation.</h2><p>The playground accepts an actual POST at an isolated, short-lived URL and returns HTTP 202. It records the body and a deliberately limited set of diagnostic headers so you can verify integration behavior without exposing credentials. Production workspaces add routing, signatures, retries, replay, contracts, and operational history.</p></section></PublicPage>; }
