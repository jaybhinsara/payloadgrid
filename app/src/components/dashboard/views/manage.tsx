import { BellRing, FileClock, KeyRound, Plus, SlidersHorizontal, Users } from "lucide-react";
import { Empty, SectionHead, timeAgo } from "@/components/dashboard/common";
import type { DashboardData, DashboardSubmit } from "@/components/dashboard/types";

export function ApiKeysView({ data, busy, submit, revoke, reveal }: { data: DashboardData; busy: string; submit: DashboardSubmit; revoke: (id: string) => void; reveal: (value: string) => void }) {
  return <><SectionHead eyebrow="Developer access" heading="API keys" copy="Keys authenticate server-to-server requests. PayloadGrid stores only a one-way hash." /><section className="split-layout"><form className="content-card form-card" onSubmit={(event) => submit(event, "/api/api-keys", (form) => ({ name: form.get("name") }), (payload) => reveal(String(payload.token || "")))}><h3>Create API key</h3><label>Key name<input name="name" required placeholder="Production server" /></label><button className="button primary" disabled={busy === "/api/api-keys"}><KeyRound size={16} /> Generate key</button></form><section className="content-card list-card">{data.apiKeys.length ? <div className="resource-list">{data.apiKeys.map((key) => <article key={key.id} className={key.revoked_at ? "disabled" : ""}><span className="resource-icon"><KeyRound size={18} /></span><div><strong>{key.name}</strong><code>{key.key_prefix}••••••••••••</code><small>Last used {timeAgo(key.last_used_at)}</small></div>{key.revoked_at ? <span>Revoked</span> : <button className="button danger small" onClick={() => revoke(key.id)}>Revoke</button>}</article>)}</div> : <Empty icon={<KeyRound size={22} />} title="No API keys" copy="Generate a key to send messages from your backend." />}</section></section></>;
}

export function TeamView({ data, busy, submit, reveal }: { data: DashboardData; busy: string; submit: DashboardSubmit; reveal: (value: string) => void }) {
  return <><SectionHead eyebrow="Organization" heading="Team access" copy="Roles keep operational access predictable across your workspace." /><section className="split-layout"><form className="content-card form-card" onSubmit={(event) => submit(event, "/api/team", (form) => ({ email: form.get("email"), role: form.get("role") }), (payload) => { if (payload.inviteUrl) reveal(String(payload.inviteUrl)); })}><h3>Add teammate</h3><label>Email<input name="email" type="email" required placeholder="developer@company.com" /></label><label>Role<select name="role"><option value="developer">Developer</option><option value="admin">Admin</option><option value="viewer">Viewer</option></select></label><button className="button primary" disabled={busy === "/api/team"}><Users size={16} /> Add or invite</button></form><section className="content-card list-card"><div className="resource-list">{data.members.map((member) => <article key={member.id}><span className="member-avatar">{member.name.slice(0, 1).toUpperCase()}</span><div><strong>{member.name}</strong><small>{member.email}</small></div><span className="role-pill">{member.role}</span></article>)}</div></section></section></>;
}

export function AutomationsView({ data, busy, submit }: { data: DashboardData; busy: string; submit: DashboardSubmit }) {
  return <><SectionHead eyebrow="Delivery controls" heading="Automations" copy="Transform payloads before delivery and define operational alert destinations." /><section className="automation-grid"><form className="content-card form-card" onSubmit={(event) => submit(event, "/api/settings", (form) => ({ kind: "transformation", name: form.get("name"), eventType: form.get("eventType") || undefined, config: { addFields: JSON.parse(String(form.get("addFields") || "{}")), removeFields: String(form.get("removeFields") || "").split(",").map((value) => value.trim()).filter(Boolean) } }))}><span className="resource-icon"><SlidersHorizontal size={18} /></span><h3>Payload transformation</h3><label>Name<input name="name" required placeholder="Add API version" /></label><label>Event type <small>Empty applies to all</small><input name="eventType" placeholder="order.created" /></label><label>Fields to add <small>JSON object</small><textarea className="code-input small" name="addFields" defaultValue="{}" /></label><label>Fields to remove <small>Comma separated</small><input name="removeFields" placeholder="internal_note, debug" /></label><button className="button primary" disabled={busy === "/api/settings"}><Plus size={16} /> Add transformation</button></form><form className="content-card form-card" onSubmit={(event) => submit(event, "/api/settings", (form) => ({ kind: "alert", name: form.get("name"), channel: form.get("channel"), destination: form.get("destination"), failureThreshold: Number(form.get("failureThreshold")) }))}><span className="resource-icon"><BellRing size={18} /></span><h3>Failure alert</h3><label>Name<input name="name" required placeholder="Production failures" /></label><label>Channel<select name="channel"><option value="email">Email</option><option value="slack">Slack webhook</option><option value="webhook">Custom webhook</option></select></label><label>Destination<input name="destination" required placeholder="ops@company.com or webhook URL" /></label><label>Failure threshold<input name="failureThreshold" type="number" min="1" max="100" defaultValue="3" /></label><button className="button primary" disabled={busy === "/api/settings"}><Plus size={16} /> Add alert</button></form></section><section className="overview-grid automation-lists"><ResourceSummary title="Transformations" count={data.transformations.length} icon={<SlidersHorizontal size={17} />} empty="Messages are delivered exactly as sent." items={data.transformations.map((item) => ({ id: item.id, name: item.name, copy: item.event_type || "All events" }))} /><ResourceSummary title="Alert rules" count={data.alerts.length} icon={<BellRing size={17} />} empty="Add a destination to prepare notifications." items={data.alerts.map((item) => ({ id: item.id, name: item.name, copy: `${item.channel} · after ${item.failure_threshold} failures` }))} /></section><section className="content-card audit-card"><div className="card-head"><div><span className="section-label">Security history</span><h3>Audit log</h3></div></div>{data.auditLogs.length ? <div className="audit-list">{data.auditLogs.map((log) => <div key={log.id}><span className="audit-dot" /><strong>{log.action.replaceAll(".", " ")}</strong><small>{log.resource_type}{log.resource_id ? ` · ${log.resource_id.slice(0, 12)}` : ""}</small><time>{timeAgo(log.created_at)}</time></div>)}</div> : <Empty icon={<FileClock size={21} />} title="No audit activity" copy="Organization changes will be recorded here." />}</section></>;
}

function ResourceSummary({ title, count, icon, empty, items }: { title: string; count: number; icon: React.ReactNode; empty: string; items: Array<{ id: string; name: string; copy: string }> }) { return <article className="content-card list-card"><div className="card-head"><h3>{title}</h3><span>{count}</span></div>{items.length ? <div className="resource-list">{items.map((item) => <article key={item.id}><span className="resource-icon">{icon}</span><div><strong>{item.name}</strong><small>{item.copy}</small></div><i className="status-dot on" /></article>)}</div> : <Empty icon={icon} title={`No ${title.toLowerCase()}`} copy={empty} />}</article>; }